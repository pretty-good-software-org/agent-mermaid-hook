# Development profile — additional tools for local work
[tools]
lefthook = "2.1.14"
gitleaks = "8.30.0"

[settings]
experimental = true

[hooks]
enter = '[ "$(git rev-parse --is-inside-work-tree 2>/dev/null)" = "true" ] && lefthook install'
