{
  "name": "@pretty-good-software-org/agent-mermaid-hook",
  "version": "0.4.0",
  "description": "Claude Code hooks that draw fenced Mermaid blocks as Unicode box art under each reply",
  "license": "MIT",
  "type": "module",
  "private": true,
  "bin": {
    "agent-mermaid-hook": "./dist/cli.js"
  },
  "prettier": "@pretty-good-software-org/ts-config/prettier.json",
  "scripts": {
    "dev": "bun run src/cli.ts",
    "build": "bun build ./src/cli.ts --outdir ./dist --target bun --format esm",
    "build:binary": "bun build ./src/cli.ts --compile --outfile ./dist/agent-mermaid-hook",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "tsc --noEmit",
    "spell": "cspell --no-progress \"**/*.{ts,md,json}\"",
    "check:lockfile": "bun install --frozen-lockfile --dry-run",
    "check": "bun run check:lockfile && bun run typecheck && bun run lint && bun run format:check && bun run spell && bun run test"
  },
  "dependencies": {
    "citty": "0.2.2",
    "grok-mermaid": "0.2.3",
    "pino": "10.3.1",
    "pino-pretty": "13.1.3",
    "zod": "4.6.5"
  },
  "devDependencies": {
    "@pretty-good-software-org/ts-config": "2.2.0",
    "@types/bun": "1.4.2",
    "@types/node": "26.5.1",
    "@typescript/native": "npm:typescript@7.0.2",
    "typescript": "npm:@typescript/typescript6@6.0.2",
    "typescript-eslint": "8.70.0",
    "eslint": "10.10.0",
    "prettier": "3.9.6",
    "cspell": "10.3.3"
  },
  "engines": {
    "bun": ">=1.1"
  }
}
